[project]
name = "shadow-teller-backend"
version = "1.0.0"
dependencies = [
    "fastapi",
    "sseclient",
    "uvicorn"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "mypy",
    "pylint",
    "types-requests"
]

